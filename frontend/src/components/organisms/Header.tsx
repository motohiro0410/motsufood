import { FC, memo } from "react";

import Box from '@mui/material/Box';

export const Header: FC = memo(() => {
  return (
    <Box sx={{ width: '100%', position: 'fixed', top: 0,height: 70, bgcolor: '#81da7f' }}>

    </Box>
    
  )
})